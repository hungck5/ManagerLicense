import Compose from "@/components/compose";

export default function Dashboard() { 
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Welcome to the dashboard!</p>
      <Compose />
    </div>
  );
}